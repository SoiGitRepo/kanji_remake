#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

cat > plan.md <<'PLAN'
# 本次会话计划与进度（自动同步）

更新时间：2025-09-26 18:08 (+08)

- 本文件用于记录本次会话的计划与执行进度；已在 `.gitignore` 中忽略，不会提交到仓库。

## 待办清单
- [x] 创建/更新会话计划并写入项目根目录 plan.md（并将其加入 .gitignore）
- [ ] 检测当前目录是否为 git 仓库（若无则初始化）
- [ ] 查看工作区改动并准备一次提交
- [ ] 执行提交（使用简体中文提交信息）
- [ ] 是否需要推送到远端（待确认远端设置）

## 说明
- 提交中将排除 `plan.md`。
- 若当前不存在 Git 仓库，将自动初始化并进行首次提交（保留你的 Git 用户名与邮箱配置，不做更改）。
PLAN

echo "plan.md 已更新。"
